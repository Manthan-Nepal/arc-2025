# Python & Data Preprocessing

## Pandas and NumPy Tasks

- Revised fundamentals of NumPy and Pandas
- Created and manipulated arrays and DataFrames
- Explored array shapes, reshaping, and broadcasting in NumPy
- Used functions like `np.unique`, `np.hsplit`, and `np.expand_dims`
- Practiced data filtering, selection, and transformation in Pandas
- Applied `.map()`, `.apply()`, and `reduce()` for data manipulation
- Sorted DataFrames by index and values

## Python Tasks

