## Divide the task in sub tasks by making functions
## Error handling 
### numpy and pandas notebook


### For python_assignments notebook
## in assignment 2.1
#### For the given question # given a list of integers x , find the peak chars. A peak is an char that is greater than its neighbors.
# # example:  x = [1,2,3,1,10,8]
# # output : peaks = [3,10]
# Logic = for the ith element in the list check i-1 and i+1 th element , the ith element should be greater than them . if yes append to the peak list else not.

### for quesno 9
# map each respective brackets with its opening ones in a dict.
## apply stack operation. check if the stack is empty or not . if top of the stack bracket is not the same as present in the dict , return not valid else pop the top of the stack until each character has been parsed.  if at the end the stack is empty return valid else not valid





