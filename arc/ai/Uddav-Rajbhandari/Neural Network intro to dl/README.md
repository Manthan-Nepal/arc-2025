# Topics
1. From ML to DL
2. Neural Networks
3. Pytorch Framework
# Objectives
1. To learn the differences between ML and DL
2. To learn the intuition, concepts and mathematics of neural networks
3. To learn pytorch framework to create and train neural networks

# Instructions
1. For the learning resource 2, you can skip lesson 4, 5, 6, 9, and 10 if you remember Logistic regression from Day 5
2. While going through the learning resource 2, if you are overwhelmed by the mathematics you can ignore the mathematical stuffs and focus on concepts and intuitions
3. For the pytorch quickstart, follow every steps exactly as given. You have to learn to create your own neural network class [class NeuralNetwork in tutorial] and to write train and test functions.
# Tasks
1. Go through all the learning resources following the instructions given
2. Create a neural network with one layer and one neuron. The data will be the truth table of AND Gate i.e it will have two inputs and one output. No need to split train/dev/test here. Using the truth table of AND gate trains the neural network. Think about the loss function to be used. Calculate the loss and accuracy. The accuracy of the model must be 100% since the data is very simple. Once the model is trained, save the model and again load the model and use it for prediction.
3. Do task 2 again for XOR gate. Document your finding.
4. What is batch, iteration and epoch?
5. Develop the simplest possible neural network to fit Iris Dataset for multi-class classification. Use pytorch and sklearn.
6. For the quickstart resource of Pytorch, the accuracy there is 50%. Your task is to boost the accuracy above 78%. Do not use transfer learning. Make sure there is no overfitting by plotting the train and test loss.
